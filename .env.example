# Copy to .env and adjust. Lines starting with # are comments.

# Port the server listens on (default 3004)
# PORT=3004

# Full base URL where users open the app — used in QR codes. Use your LAN IP or hostname for phones.
# No trailing slash.
# BASE_URL=http://192.168.1.50:3004
